# Self-Healing CI Fix Context

This document provides context about a CI fix generated by Nx Cloud's Self-Healing CI.

## Pull Request Context

**Title:** feat: import open-forge-pipeline as @open-forge/pipeline
**Description:** ## Summary

Imports the `open-forge-pipeline` repository into this monorepo as `@open-forge/pipeline` under `packages/pipeline/`.

### What was done

- **Imported with full git history** via `nx import`
- **Renamed** package from `open-forge-pipeline` to `@open-forge/pipeline`
- **Removed standalone repo tooling** — husky, commitlint, lint-staged, CI workflow, .opencode skills/commands
- **Stripped npm scripts** — Nx infers `build`, `test`, `lint`, `typecheck` from config files
- \*\*Added Nx tscon...

## Tasks Being Fixed

- `nx-cloud record -- npx nx format:check --base=remotes/origin/main`

## Error Summary

TASK ID: nx-cloud record -- npx nx format:check --base=remotes/origin/main
docs/REQUIREMENTS_TEMPLATE.md
docs/REQUIREMENTS_TEMPLATE_LITE.md
packages/pipeline/openspec/changes/archive/2026-04-02-phase-3-handoff-helpers/design.md

## Analysis & Reasoning

The `nx-cloud record -- npx nx format:check` task failed because three files introduced by this PR were not formatted according to the workspace's Prettier configuration.

The files in question are:

- `docs/REQUIREMENTS_TEMPLATE.md`
- `docs/REQUIREMENTS_TEMPLATE_LITE.md`
- `packages/pipeline/openspec/changes/archive/2026-04-02-phase-3-handoff-helpers/design.md`

Per the classification rules, `format:check` failures are always classified as `code_change`, since they are a direct result of code/content introduced in the PR.

The fix was applied by running `npx nx format --base=remotes/origin/main`, which is the standard Nx command for auto-formatting all files changed relative to the main branch. No manual edits were made — Prettier rewrote the files in-place to match the monorepo's formatting rules (e.g., line width, trailing newlines, consistent Markdown style).

This approach was chosen over manually editing individual files because it is the canonical, reliable way to apply workspace formatting and avoids introducing human error. It also mirrors the existing commit history pattern (see `style(pipeline): format imported files to match monorepo prettier config`), confirming this is an expected and recurring need when importing external files into the monorepo.

---

_Generated by Nx Cloud Self-Healing CI_
